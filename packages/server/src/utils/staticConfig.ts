export const config = {
  socialHub: {
    /**
     * This must match the 'webhook.secret' found in the manifest document used by the channel
     */
    manifestSecret: 'should be at least 32 chars'
  }
}